import { atom } from "recoil";

export const myInfo = atom({
  key: "myInfo",
  default: null,
});

export const myEasId = atom({
  key: "myEasId",
  default: null,
});

export const myPrefix = atom({
  key: "myPrefix",
  default: null,
});
